{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/bowl-of-ramen.exe",
            "args": ["dev"],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "preLaunchTask": "cargo build"
        }
    ]
}